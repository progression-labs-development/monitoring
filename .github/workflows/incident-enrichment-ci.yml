name: Incident Enrichment CI

on:
  push:
    paths:
      - 'services/incident-enrichment/**'
      - '.github/workflows/incident-enrichment-ci.yml'
  pull_request:
    paths:
      - 'services/incident-enrichment/**'
      - '.github/workflows/incident-enrichment-ci.yml'

permissions:
  contents: read

jobs:
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: services/incident-enrichment/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: services/incident-enrichment
      - run: pnpm typecheck
        working-directory: services/incident-enrichment

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: services/incident-enrichment/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: services/incident-enrichment
      - run: pnpm test
        working-directory: services/incident-enrichment

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: services/incident-enrichment
          push: false
