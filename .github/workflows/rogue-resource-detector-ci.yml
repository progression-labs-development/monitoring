name: Rogue Resource Detector CI

on:
  push:
    paths:
      - 'services/rogue-resource-detector/**'
      - '.github/workflows/rogue-resource-detector-ci.yml'
  pull_request:
    paths:
      - 'services/rogue-resource-detector/**'
      - '.github/workflows/rogue-resource-detector-ci.yml'

permissions:
  contents: read

jobs:
  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: services/rogue-resource-detector/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: services/rogue-resource-detector
      - run: pnpm typecheck
        working-directory: services/rogue-resource-detector

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: services/rogue-resource-detector/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: services/rogue-resource-detector
      - run: pnpm test
        working-directory: services/rogue-resource-detector

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: services/rogue-resource-detector
          push: false
